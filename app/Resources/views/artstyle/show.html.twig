{% extends 'base.html.twig' %}

{% block body %}
    <h1>Artstyle</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artStyle.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ artStyle.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('artstyle_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('artstyle_edit', { 'id': artStyle.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
