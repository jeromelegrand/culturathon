{% extends 'base.html.twig' %}

{% block body %}
    <div class="text-center blackground">
        <h1>Nouveau Style</h1>
    </div>

    <div class="blackground mt-3">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="text-center">
            <input class="btn btn-blue" type="submit" value="Ajouter" />
            <a class="btn btn-green" href="{{ path('artstyle_index') }}">Retour</a>
        </div>
        {{ form_end(form) }}
    </div>

{% endblock %}
